#include <bits/stdc++.h>
using namespace std;

void perHelper(vector<string>&res,string s,int st,int e){
    if(st>=e){
        res.push_back(s);
        return;
    }
    
    
    for(int i=st;i<=e;i++){
        swap(s[i],s[st]);
        perHelper(res,s,st+1,e);
        swap(s[i],s[st]);
    }
}

vector<string> generatePermutation(string s){
    sort(s.begin(),s.end());
    vector<string>res;
    perHelper(res,s,0,s.length()-1);
    return res;
    
}

int main() {
	//code
	int t;
	cin>>t;
	while(t--){
	    string s;
	    cin>>s;
	    vector<string>res=generatePermutation(s);
	    for(string x :res){
	        cout<<x<<" ";
	        
	    }
	    cout<<"\n";
	}
	return 0;
}
